# Python 编码规范

- 遵循现有代码风格和 Python 社区惯例，优先保证可读性。
- 新增或修改的公共函数、方法和复杂内部函数应提供类型标注。
- 使用清晰、具体的英文名称；避免无意义缩写和模糊命名。
- 函数和类保持单一职责，复杂流程拆分为职责明确的小单元。
- 优先使用已有抽象和标准库，不重复实现现有能力。
- 异常处理应针对可预期异常，不使用空的 `except` 或无差别吞掉异常。
- 注释解释原因、约束或非显而易见的决策，不复述代码。
- 默认使用 ASCII；仅在用户可见文本、文档或已有文件需要时使用 Unicode。
- 不为当前需求之外的假设场景提前增加抽象。
